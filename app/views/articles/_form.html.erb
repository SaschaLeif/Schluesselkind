<%= form_for(@article) do |f| %>
<% if @article.errors.any? %>
<div id="error_explanation">
	<h2><%= pluralize(@article.errors.count, "error") %> prohibited this article from being saved:</h2>

	<ul>
		<% @article.errors.full_messages.each do |msg| %>
		<li>
			<%= msg %>
		</li>
		<% end %>
	</ul>
</div>
<% end %>

hier wird nix geändert!
<div id="gross">
	<div class="field">
		<%= f.label 'Art:' %>
		<%= f.select :name,
      [
        ['T-Shirt gelb', 'T-Shirt gelb'],
        ['T-Shirt orange', 'T-Shirt orange'],
        ['T-Shirt schwarz', 'T-Shirt schwarz'],
        ['T-Shirt weiss', 'T-Shirt weiss']
      ] %>
	</div>
<br />	
	<div class="field">
		<%= f.label 'Geschlecht: ' %>
		<%= f.select :gender,
      [
        ['männlich', 'Mann'],
        ['weiblich', 'Frau']
      ] %>
	</div>
<br />
	<div class="field">
		<%= f.label 'Motiv url: ' %>
		<%= f.text_field :article_url %>
	</div>

	<br />

	<div class="actions">
		<%= f.submit %>
	</div>
</div>
<br />
<% end %>

