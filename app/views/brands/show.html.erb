<br />

<%= image_tag (@brand.logo_url) %>

<h1> <%= @brand.name %> </h1>

<div id="gross">
	<%= raw @brand.info_text %>
</div>

<br />

<div id="klein">
	<%= link_to 'editiere', edit_brand_path(@brand) %> |

	<%= link_to 'zurück', brands_path %>
</div>