<%= form_for(@custom_product) do |f| %>
  <% if @custom_product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@custom_product.errors.count, "error") %> prohibited this custom_product from being saved:</h2>

      <ul>
      <% @custom_product.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :product_id %><br />
    <%= f.number_field :product_id %>
  </div>
  <div class="field">
    <%= f.label :print_id %><br />
    <%= f.number_field :print_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
